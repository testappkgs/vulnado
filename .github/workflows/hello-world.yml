name: Hello-World
on: [push]

jobs:
    run-shell-command:
        runs-on: ubuntu-latest
        steps:
            - name: echo a string
              run: echo "Hello, KPMGins! This is our First GitHub Action"